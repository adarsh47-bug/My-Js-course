{
  "name": "starter",
  "version": "1.0.0",
  "description": "",
  "browserslist": "last 2 versions, > 0.5% and ie >= 11",
  "scripts": {
    "start": "rm -rf .parcel-cache && parcel index.html --target default",
    "build": "rm -rf .parcel-cache && parcel build index.html --no-optimize"
  },
  "author": "Adarsh",
  "license": "ISC",
  "dependencies": {
    "core-js": "^3.36.0",
    "leaflet": "^1.9.4",
    "lodash-es": "^4.17.21",
    "regenerator-runtime": "^0.14.1"
  },
  "devDependencies": {
    "parcel": "^2.11.0"
  }
}